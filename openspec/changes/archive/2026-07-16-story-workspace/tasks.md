## 1. 工作区与元数据模型

- [x] 1.1 定义工作区概念与边界（一本书=一个自包含项目）
- [x] 1.2 定义元数据字段：必填（书名、体裁、语言）与可选可扩展字段（简介、目标读者、基调等）
- [x] 1.3 定义打开/创建工作区的加载契约（含元数据与章节树装载）

## 2. 正文数据模型

- [x] 2.1 定义章节树弹性层级（卷可选 / 章必需 / 场景可选）
- [x] 2.2 定义稳定唯一标识符方案及其与标题/顺序/内容解耦的规则
- [x] 2.3 定义章级草稿的读写契约（经标识符定位）
- [x] 2.4 明确标识符作为 bug 定位/事实溯源/diff/time-travel 长期引用锚点的对外约定

## 3. 导入既有小说

- [x] 3.1 定义导入解析的保真规则（逐字保留原文，禁止自动改写）
- [x] 3.2 定义边界识别策略（标题层级/文件组织 → 卷/章/标题）
- [x] 3.3 定义歧义与失败的人工确认降级流程
- [x] 3.4 定义来源路径记录与再导入约定
- [x] 3.5 明确大文档解析归属 utilityProcess（遵循 process-model）

## 4. 本地存储布局

- [x] 4.1 定义正文 Markdown 落盘与人类可读结构/元数据存储形式
- [x] 4.2 定义标识符持久化方式及与文件映射的鲁棒性策略
- [x] 4.3 定义手工改动后重建映射的检测与途径
- [x] 4.4 明确版本控制友好（可 diff 文本）的布局约束

## 5. 校验

- [x] 5.1 `openspec validate story-workspace --strict` 通过
- [x] 5.2 确认与 bootstrap-foundation（进程/IPC/规范）的依赖引用一致
- [x] 5.3 用自带示例 `津门余味` 目录作为导入解析的验收参考用例（规划记录，非实现）
