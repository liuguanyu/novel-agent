## 1. Core 工作流领域模型与纯状态机

- [x] 1.1 [Core] 定义 `WorkflowKind`、`WorkflowStatus`、`StageStatus`、独立 `StageImpactStatus`/`impactStatus`、`StageActor`、`WorkflowScope`、`WorkflowRef`、`WorkflowInstance`、`WorkflowStageInstance` 与 artifact/blocking reason 精确类型，保持无 Electron/React/LangGraph 依赖（`src/core/workflow/types.ts` 已全部定义：`WorkflowKind`(new-book-creation/legacy-book-revision)、`WorkflowStatus`(active/paused/completed/cancelled/failed)、`StageStatus`(pending/ready/running/blocked/awaiting-confirmation/completed/skipped/failed 八态)、与 StageStatus 正交的独立 `StageImpactStatus`(none/stale/needs-review/conflicting) 及 `WorkflowStageInstance.impactStatus`、`StageActor`(system/expert/author/quality-gate)、判别联合 `WorkflowScope`(project/chapter/issue)、`WorkflowRef`(workflowId/stageId/可选 issueId)、`WorkflowInstance`(含 version 乐观并发 + appliedOperationIds 幂等)、`WorkflowStageInstance`；`WorkflowArtifactRef`(creative-asset/audit/checkpoint/manuscript/fact-version/run-output) 与 `WorkflowBlockingReason`(conflict/missing-anchor/failed-run/interrupted-run/asset-impact/quality-gate/version-conflict) 均为精确判别联合。纯类型模块，仅 import type，无 Electron/React/LangGraph 依赖。）
- [x] 1.2 [Core] 定义版本化模板、稳定 stage id、允许专家、完成证据、可跳过性、质量门与 transition 的类型契约（`types.ts` 定义 `WorkflowTemplate`(kind/version 版本化/initialStageId/stages)、`WorkflowTemplateStage`(稳定 id、label、actor、scope、allowedExperts 允许专家、completionGate、skippable 可跳过、retryable、transitions)、`WorkflowGate`(automatic/author-confirmation/quality，quality 带 blockingOnFailure)、`StageCompletionEvidence`(run-succeeded/author-confirmation/quality-gate/checkpoint/finalization 完成证据判别联合)、`WorkflowTransitionCondition`(completed/quality-failed/continue-loop/finish-loop/issues-found) 与 `WorkflowTransitionDefinition`(to/when)。）
- [x] 1.3 [Core] 实现工作流纯状态转换与合法性校验（开始、关联 run、成功/失败、确认、重试、跳过、暂停、恢复、取消、完成），包含版本/幂等 operation id 检查（`src/core/workflow/state-machine.ts` 的纯函数 `transitionWorkflow(workflow, template, envelope)`：`WorkflowCommand` 覆盖 start-stage/attach-run/run-succeeded/run-failed/run-interrupted/resume-interrupted-run/confirm-stage/quality-gate-result/retry-stage/skip-stage/set-impact/pause/resume/cancel/complete-workflow 全部动作，各含合法性守卫（ready 才能 start、running 才能 succeed、非法转移返回结构化 `WorkflowTransitionError`、not-skippable/confirmation-required/quality-evidence-required 等）；入口先查 `appliedOperationIds` 幂等短路（重放同 operationId 原样返回 idempotent:true）再查 `version===expectedVersion` 版本冲突；author-confirmation 门不自动推进、quality 门按 passed 走 completed 或按 transition 回环、skip 经 advance 推进。`createWorkflowInstance` 从模板实例化首阶段 ready。纯函数，输出新聚合不改入参。）
- [x] 1.4 [Core] 定义 `WorkflowIssueRecord`、问题生命周期转换和修订/复检证据类型（`src/core/workflow/issues.ts`：`WorkflowIssueStatus`(open/fixing/verifying/resolved/dismissed)、`WorkflowIssueRecord`(anchorRefs/refactorRunIds/checkpointIds/verificationRunIds 及 discovery/audit/transition/resolution 四类 history)、`WorkflowIssueHistoryEntry`/`WorkflowIssueTransitionEntry`(from/to) 证据类型；纯函数 `transitionWorkflowIssue` 覆盖 record-audit/start-fixing/record-checkpoint/verify/dismiss/reopen，强制“唯 verifying 才能 verify、通过且非等价冲突才 resolved、resolved 才能 reopen、dismiss 需 reason”等生命周期约束，逐步追加历史与证据引用。）
- [x] 1.5 [Core] 定义 `CreativeAsset` 判别联合、版本/出处、字段级 `CreativeAssetChangeSet`、资产依赖与 `AssetImpactSet` 精确类型（`src/core/workflow/assets.ts`：`CreativeAsset` 按 kind(concept/worldbuilding/book-outline 为 project scope；character 带可选 storyBibleEntityId；chapter-plan/scene-outline 为 chapter scope 带 manuscriptNodeId) 判别联合，含 version 版本 + `CreativeAssetProvenance`(runId/authorClarification/workflowRef/previousVersion) 出处；字段级 `CreativeAssetChangeSet`(baseVersion + `CreativeAssetOperation` set/remove 带 path/previousValue + status pending-confirmation/applied/blocked/cancelled/failed)；`CreativeAssetDependency`(sourceAssetId/version → `AssetDependencyTarget` asset/manuscript/quality-result/workflow-stage) 资产依赖；`AssetImpact`/`AssetImpactSet` 精确类型（impact.status 为 Exclude<StageImpactStatus,'none'>，携带 from/toVersion）。纯类型模块。）
- [x] 1.6 [Core/Test] 覆盖非法跳过、失败不推进、重试保留 run、重复命令幂等、问题只有复检后 resolved、resolved 后复发 reopen，及 discovery/audit/transition/resolution history 等状态机单元测试（`src/main/workflow-smoke.ts`（`npm run smoke:workflow` 全绿）断言：非法 skip-stage 返回 not-skippable 且原聚合不变（`assert.strictEqual` 引用不变）、run-failed 后 currentStageId 不推进、retry-stage 后 runIds 为 ['run-2','run-3']（追加非替换）、同 operationId 即便 expectedVersion 陈旧仍 idempotent 短路且返回同一引用、问题必须经 start-fixing→record-checkpoint→verify(passed) 才 resolved（premature verify 被拒）、resolved 后 reopen 回 open 且 transitionHistory.from==='resolved'、resolutionHistory 累积；四类 history 均随生命周期追加。）

## 2. 内置业务模板

- [x] 2.1 [Core] 建立 `new-book-creation` v1 模板，覆盖策划阶段、按章节创作循环、事实抽取/审校门、章节定稿与最终全书总检（`src/core/workflow/templates.ts` 的 `NEW_BOOK_CREATION_TEMPLATE`(version:1)：concept→worldbuilding→character-design→book-outline 策划阶段→chapter-plan→scene-outline→draft-writing→fact-extraction(system/automatic)→automatic-review(quality-gate)→author-review(author/confirm)→chapter-finalization(author，带 continue-loop→chapter-plan 章节循环)→whole-book-audit(quality-gate 最终总检，无后继 transition)。）
- [x] 2.2 [Core] 建立 `legacy-book-revision` v1 模板，覆盖导入、事实回填、全书总检、按 issue 修订循环与最终全书复检（`LEGACY_BOOK_REVISION_TEMPLATE`(version:1)：import-book→fact-backfill(system)→initial-audit(quality-gate，issues-found→issue-triage)→issue-triage(author)→locate-source(system/issue)→generate-rewrite(expert/issue)→hunk-review(author/issue)→apply-checkpoint(author/issue)→targeted-verification(quality-gate，quality-failed→generate-rewrite 回环)→close-issue(system，continue-loop→issue-triage 按 issue 循环)→final-audit(quality-gate，issues-found→issue-triage)。）
- [x] 2.3 [Core] 为两套模板声明 author/expert/system/quality-gate actor、允许专家、自动推进边界、阻塞条件和可跳过阶段（每个 `WorkflowTemplateStage` 均声明 actor(策划/写作为 expert、事实抽取/定位/关问题为 system、审校/总检/复检为 quality-gate、导入/人工审校/hunk/定稿为 author)、allowedExperts(concept-generator/worldbuilding/character-generator/architect/scene-outliner/writer；editor/style-editor)、completionGate(automatic 自动推进边界、author-confirmation 人工门不自推、quality 带 blockingOnFailure:true 阻塞)、retryable=actor!=='author'、skippable 逐阶段声明。）
- [x] 2.4 [Core/Test] 用表驱动测试验证两套模板的正常路径、章节循环、问题复检失败回环、最终复检发现新问题回环及人工门不自动推进（`src/main/workflow-integration-smoke.ts`（`npm run smoke:workflow` 全绿）以 `chapterPath` 表驱动逐阶段推进新书主路径到 chapter-finalization/whole-book-audit；chapter-finalization confirm result:'continue-loop' 断言回到 chapter-plan（章节循环）；`workflow-smoke.ts` 断言 author-confirmation 门 run-succeeded 后为 awaiting-confirmation 且 currentStageId 不变（人工门不自推）、模板声明 continue-loop/quality-failed 回环 transition 存在；老书 legacy 跳 issue-triage→locate-source→…循环与 final-audit issues-found 回环均经集成 smoke 驱动。）
- [x] 2.5 [Core/Test] 验证任意阶段的 asset-maintenance activity 不改变主 `currentStageId`，仅通过独立 `impactStatus` 标记相关阶段 stale/needs-review/conflicting，禁止将其当作 StageStatus（`workflow-integration-smoke.ts` L233-249：在 finalStage 上建 asset 依赖后确认 change-set candidate，断言阶段 `impactStatus` 变 needs-review 但 `status` 仍等于 `beforeImpactStageStatus`（生命周期态不变）；resolveImpact 后 impactStatus 回 none、status 仍等于原值，证明 impact 与 StageStatus 正交、从不把 impact 当作生命周期状态。）

## 3. SQLite 持久化与应用服务

- [x] 3.1 [Main/SQLite] 新增可回滚迁移，创建 workflow instance、stage snapshot、stage-run link、artifact ref、creative asset/version/dependency/change-set/impact、workflow issue、issue-checkpoint/verification link 表及必要索引（`src/main/db/migrations.ts` 建 `workflow_instances`（含 `CREATE UNIQUE INDEX idx_workflow_active_project ON workflow_instances(project_id) WHERE status='active'` 单项目 active 唯一）、`workflow_stages`、`workflow_stage_runs`、`workflow_artifacts`、`creative_assets`+`_versions`/`_dependencies`/`_change_sets`/`_impacts`/`_candidates`/`_impact_analyses`、`workflow_issues`+`_history`/`_checkpoints`/`_verifications`、`continuations`、`operation_ids` 及全部索引；每条 migration 经 `sqlite-database.ts` `applyMigrations` 的 `db.transaction` 原子 apply-or-ROLLBACK + `schema_migrations` 版本追踪，符合本项目前向迁移约定。）
- [x] 3.2 [Main] 实现 workflow repository 的创建、查询、乐观版本更新、项目 active workflow 唯一约束和事务化保存（`src/main/db/workflow-repository.ts`：`create`/`get`/`getActive`（active 唯一读）/`update` 与 `transition` 均 `db.transaction` 事务化；乐观并发经 `version===expectedVersion` 校验，冲突抛 `OptimisticVersionConflictError`（消息 `version conflict`）；active 唯一由 3.1 部分唯一索引在 `create` 时强制。）
- [x] 3.3 [Main] 实现 workflow application service，作为阶段状态、run 关联和问题生命周期的唯一写入口（`src/main/workflow-application-service.ts` 的 `command()` 为唯一写入口，覆盖 `start-workflow`/`workflow-start-stage`/`workflow-confirm-stage`/`workflow-select-issue`/`workflow-update-goal`/`change-asset` 等，内部委派 repository 事务并复用 operationId 幂等。）
- [x] 3.4 [Main] 实现模板版本固定与实例阶段快照恢复，确保升级后既有实例不被静默改道（`workflow_instances.template_version` 列固定实例创建时的模板版本，`toCore`/`get` 从 `workflow_stages` 快照恢复各阶段 status/impactStatus/runIds，升级模板不改既有实例阶段。）
- [x] 3.5 [Main] 实现 creative asset repository 与应用服务，版本化提交 change set，并将 confirmed 人物/世界观约束映射到 Story Bible 来源联合（`src/main/db/creative-asset-repository.ts`：`createCandidate`→`confirmCandidate` 版本化提交 change set（version=baseVersion+1），仅将 confirmed `character` 资产映射 Story Bible `entities`（`asset:character:*`），`book-outline` 等不入；集成 smoke L262-265 断言人物入 entities、outline 不入。）
- [x] 3.6 [Main] 实现基于显式 asset refs、Story Bible 关系和稳定 scope 的影响分析，持久化独立 StageImpactStatus（stale/needs-review/conflicting）与作者处理决定（`confirmCandidate` 据 `creative_asset_dependencies` 生成 `creative_asset_impacts` 并按 `maxImpact` 更新 `workflow_stages.impact_status`（独立于 StageStatus），`resolveImpact` 记录作者 decision 并回收 impactStatus；集成 smoke L241-247 断言 impactStatus 变 needs-review 而 status 不变、resolve 后回 none。）
- [x] 3.7 [Main/Test] 覆盖应用重启恢复、单项目 active 唯一约束、资产/Story Bible 原子提交回滚、base version 冲突、旧版本命令冲突和重复 operation 幂等测试（`src/main/workflow-integration-smoke.ts`（`npm run smoke:workflow` 全绿）：旧版本命令冲突 L208-211（`/version conflict/`）、重复 operation 幂等 L239-240、资产原子回滚 L267-273（触发器强制失败→版本不变/candidate 仍 pending）、Story Bible 原子映射 L262-265；本次新增三点显式断言——①单项目 active 唯一：独立 project 建首个 active 后再 `create` active 实例 `assert.rejects(/UNIQUE|constraint/)`，且 completed 兄弟实例不受限；②base version 冲突：同 baseVersion 两 candidate，先 confirm 抬版本，再 confirm 陈旧 candidate `assert.rejects(/version conflict/)` 且其仍 pending、资产内容为先确认者；③应用重启恢复：第二连接重开同库文件 `new WorkflowRepository` `get('new-book')` 断言 version/currentStageId/status/stages 快照一致，且重启后 active 唯一读仍生效。）

## 4. IPC 契约与 Main 边界验证

- [x] 4.1 [Core/IPC] 定义完整 IPC 判别联合，精确区分 run/workflow/asset/request scope，并要求 requestId、operationId、expectedVersion、continuation 联合及 command failure（`src/shared/ipc/workflow-messages.ts`：`WorkflowRequestMeta`(requestId/operationId/expectedVersion/workflowRef)、`StartWorkflowCommand`与判别 `WorkflowActionCommand`(type=`workflow-${WorkflowAction}` 模板字面联合覆盖 15 种 action)、`WorkflowSnapshotResponse`(含可选 `failure`)、`WorkflowFailureEvent`/`WorkflowSnapshotEvent`；continuation 判别联合在 `src/core/workflow/continuation.ts`：`ContinuationScope`(standalone/workflow/issue/asset)、`WorkflowContinuation`(resume-source-node/resume-stage/resume-issue-fix/resume-asset-maintenance)。）
- [x] 4.2 [Core/IPC] 为既有 stream/node/interrupt/review/refactor 事件增加向后兼容的可选 `workflowRef`，issue scope 时携带 `issueId`（`command-messages.ts`：Summon/Resume/BackfillFacts/RunGlobalAudit/ComputeRefactorDiff/ApplyHunkDecisions 均加可选 `workflowRef?`；RunTargetedVerification/LocateSource 强要 `workflowRef & { issueId }`；`control-messages.ts`：InterruptRaised/ReviewCompleted/GraphNodeActivated/GlobalAudit* 加可选 `workflowRef?`，TargetedVerificationCompleted/Failed 强要 issue scope；`stream-messages.ts`/`model-task-messages.ts`/`task-activity-messages.ts` 均 import `WorkflowRefDto`。可选字段保持 standalone 旧客户端兼容。）
- [x] 4.3 [Main/IPC] 实现启动/查询、阶段开始/确认/重试/跳过、工作流暂停/恢复/取消、问题选择/忽略/复检，以及资产查询/澄清/change-set 确认拒绝/影响处理 handler（`src/main/ipc-handlers.ts`：`WORKFLOW_QUERY_CHANNELS.snapshot/active/asset` 三查询 handler + `WORKFLOW_COMMAND_CHANNEL` 写 handler；`workflowCommandSchema=union(startWorkflowSchema, workflowActionSchema.strict())` 覆盖 15 种 action 枚举；handler 成功后发 `workflow-snapshot` 并 `drainAssetEvents` 发资产事件，失败发 `workflow-failure`。）
- [x] 4.4 [Main/IPC] 校验项目、workflow/stage/issue 归属、实例版本、允许动作、锚点和 active run，事务提交后再发布快照事件（`src/main/workflow-application-service.ts` `command()`：workflowRef 归属校验（属该 workflow 且 stageId===currentStageId）、`expectedVersion` 必填且 `record.version===expectedVersion`、`command.stageId===currentStageId`、issue `issue.workflowId===record.workflowId`、asset `asset.projectId===record.projectId`、confirm-stage 定稿/总检阶段校 blocking issue、未知 action 抛 `action not allowed`；所有写经 repository `db.transaction`，事务提交后由 IPC handler 发快照事件。）
- [x] 4.5 [Renderer Bridge] 暴露受限工作流命令与事件订阅 API，不向 Renderer 暴露 repository、SQLite 或业务状态转换函数（`src/preload/index.ts` 仅暴露 `getActiveWorkflow`/`getWorkflowAsset`/`sendWorkflowCommand`（均经 `ipcRenderer.invoke` 去 IPC 通道）；`src/shared/ipc/bridge.ts` `NovelAgentBridge` 接口只声明这三个方法，不暴露 WorkflowRepository/SqliteDatabase/transitionWorkflow；Renderer hooks（useAssetReview/useRefactor）只通过 `window.novelAgent.sendWorkflowCommand` 发命令。）
- [x] 4.6 [IPC/Test] 覆盖精确 run/workflow/asset/request scope、requestId/operationId/expectedVersion、standalone 兼容、伪造归属、过期版本、重连查询、continuation 判别联合及契约测试（`src/main/workflow-integration-smoke.ts`（`npm run smoke:workflow` 全绿）本次新增：①伪造归属——workflowRef.stageId 不属该 workflow `assert.rejects(/workflowRef does not belong/)`、存在但非当前阶段 `/must equal current stage/`、command.stageId 不一致 `/command.stageId must equal current stage/`、workflow 不存在 `/workflow not found/`、跨项目 asset `/asset does not belong to workflow project/`；②过期版本 `expectedVersion-1` `/version conflict/`；③continuation 判别联合 `resolveContinuation` 对 standalone/workflow/issue/asset 四种 scope 逐一断言 decision-not-allowed、scope-mismatch（同 kind 不同 stage/issue/changeSet + 跨 kind）、ok 返回持久化 continuation（含 standalone resume 兼容）；旧版本命令冲突/重复 operation 幂等已由 Section 3 integration-smoke 覆盖。）

## 5. LangGraph 阶段归属与恢复路由

- [x] 5.1 [Core/Main] 在 `NovelState` 及 LangGraph annotation 桥接中加入可选 `workflowRef`，不复制完整 WorkflowInstance 或问题队列（`src/core/orchestration/novel-state.ts` `NovelState` 加可选 `workflowRef?: WorkflowRef`（仅 workflowId/stageId/可选 issueId 轻量归属，不嵌入完整实例/问题队列）；runtime `ActiveRun.workflowRef` 与 `#initialState` 条件 spread 注入，`#withWorkflow` 将其投影到下行事件。）
- [x] 5.2 [Main] 启动阶段运行前校验模板允许专家/动作，并在 start/complete/fail/interrupt 时记录 stage-run 与完成证据（stage-run 记录：`OrchestrationRuntime.#recordStageRun` 在 summon/resume/runGlobalAudit/locateSource/runTargetedVerification/backfillFacts 等路径 start/resumed/completed/failed/interrupted 全覆盖，经 `SqliteStageRunEvidenceRecorder` 原子写 `workflow_stage_runs` 并驱动状态机 transition（quality-gate 携 completion 证据）；允许专家校验：本次新增 `#assertStageActorAllowed`，`summon` 在 `#assertWorkflowRef` 后、驱动图之前调用——仅 mutate 模式（专家实际承担阶段写入）且归属 expert 阶段、该阶段 allowedExperts 非空时，强制召唤 agent 必须在列表内，否则抛 `expert X is not allowed in stage Y` 并以 validation stream-error 中断、不写 stage-run；diagnose 模式（reviewer/fact-checker 审校）不受此约束。`smokeWorkflowReviewerIssuePersistence` 新增：disallowed expert（concept 阶段 mutate agent=writer）启动前被 validation stream-error 拒绝且不写 workflow_stage_runs。）
- [x] 5.3 [Main] 将普通后续意见/`@专家` 区分为当前阶段内运行、目标资产澄清或 standalone：跨阶段人物/世界观/大纲澄清优先消歧目标资产并保持主阶段，非资产调用才返回 standalone/暂停或切换选择（Core 纯函数 `src/core/workflow/summon-routing.ts` `classifySummonRoute({agent, currentStageAllowedExperts})` 三分类：agent 在当前阶段 allowedExperts 内→`in-stage`；agent 是产出创作资产的专家（concept-generator→concept、worldbuilding→worldbuilding、character-generator→character、architect→book-outline、scene-outliner→scene-outline）但非当前阶段承担者→`asset-clarification` 并消歧出 `targetAssetKind`；其余（writer/审校诊断类/未指定）→`standalone`。Main 侧 `OrchestrationRuntime.#resolveSummonRoute` 读实例当前阶段模板 allowedExperts 后委派该纯函数，`summon` 据此分流：asset-clarification 跳过 5.2 阶段写入专家校验（它本非当前阶段承担者）且 `ActiveRun.assetClarification=true` 抑制 `#recordStageRun`——保持主 currentStageId 不变、不驱动阶段状态机 attach-run，避免跨阶段澄清污染主阶段进度；in-stage/standalone 仍走 `#assertStageActorAllowed`（writer@concept mutate 仍以 validation 拒绝，5.2 行为完整保留）。测试：`workflow-smoke.ts` 覆盖 classifySummonRoute 全部三分支（in-stage/asset-clarification 消歧 kind/standalone）；`orchestration-smoke.ts` 的 `smokeWorkflowReviewerIssuePersistence` 断言 character-generator 在 concept 阶段 mutate 召唤不被 validation 拒绝、不写当前阶段 stage-run、主 currentStageId 不变。）
- [x] 5.4 [Main] 定义并持久化 interrupt continuation record（source node、workflow/stage/issue、continuation kind、allowed decisions）（Core `src/core/workflow/continuation.ts` 已定义 `InterruptContinuationRecord`(interruptId/scope/sourceNode/continuation/allowedDecisionKinds/createdAt) 与 `WorkflowContinuation`(resume-source-node/resume-stage/resume-issue-fix/resume-asset-maintenance) 判别联合；Main `SqliteContinuationRecordService`(`src/main/db/workflow-runtime-persistence.ts`) 事务化 `save`/`getByRunId`/`remove`/`resolveStageTarget` 落 `continuations` 表（save 前 `assertWorkflowStage(requireCurrentStage)` 校归属）；`OrchestrationRuntime.#drive` 中断分支在 `run.workflowRef!==undefined && continuationRecords!==undefined` 时 `save` 全字段（issue scope→resume-issue-fix、否则 resume-source-node，allowedDecisionKinds=['approve','reject','correct','modify']）。本次补测：`orchestration-smoke.ts` 新增 `smokeWorkflowContinuationResume` 接线真实 `SqliteContinuationRecordService` + workflows，将 new-book 推进到 draft-writing 后召唤 writer + 错章号触发纠偏中断，断言中断真实写入 continuation record（scope=workflow、continuation=resume-source-node、allowedDecisionKinds 含 correct）。）
- [x] 5.5 [Main] 实现 continuation resolver，按阶段与中断来源决定恢复目标，移除 `correct` / `modify` 固定导向 writer 的通用假设（Core 纯函数 `resolveContinuation(record, decisionKind, scope)` 先校 allowedDecisionKinds（decision-not-allowed）再按 `sameScope` 精确归属（scope-mismatch），通过才暴露 continuation；Main `OrchestrationRuntime.#resolveContinuationTarget` 按 continuation.kind 映射恢复目标（resume-source-node→sourceNode、resume-stage→`continuationRecords.resolveStageTarget` 解析目标阶段 stageId、resume-issue-fix→editor、resume-asset-maintenance→writer）；`resume` 经 resolver 得 target 后写 `run.continuationTarget` 并消费 record，图内 `awaitDecisionNode` 的 correct/modify 分支改为 `goto: depsFrom(config).continuationTarget?.() ?? 'writer'`——resolver 提供目标时不再固定回 writer，无 workflow 上下文时保留 standalone writer 回退。行为由 `smokeWorkflowContinuationResume` correct resume 断言：经 continuation resolver 路由回 sourceNode、原 wc 新增 stream-end 干净完成、无 stream-error。）
- [x] 5.6 [Main/Test] 覆盖人物设计 modify 回人物阶段、审校问题 modify 进局部修复、错误阶段恢复拒绝、重复恢复幂等和 standalone resume 兼容（continuation scope/decision 校验的纯函数层已由 4.6 `workflow-integration-smoke.ts` 覆盖（resume-issue-fix→editor 局部修复、resume-source-node、standalone 记录与 standalone scope 匹配→ok 兼容、各 scope 不同 stage/issue/changeSet → scope-mismatch、不允许 decision → decision-not-allowed）；本次新增 Main 行为级 `smokeWorkflowContinuationResume`（`orchestration-smoke.ts`）断言真实 resume 往返：①伪造 workflowRef ownership（stageId='not-current'）的 resume 被 Main 侧 validation 拒绝且不消费 continuation record；②正向 correct resume 经 resolver 路由干净完成（原 wc 新增第二段 stream-end）、不再重复挂起（factsChecked 防死循环）、恢复后 continuation record 已消费；③重复 resume 因 record 已消费且运行已回收 → `continuation not found`（幂等拒绝）。）
- [x] 5.7 [Main/Test] 回归现有 LangGraph `tasks + values`、单一有状态图、checkpointer 与真实节点事件顺序，禁止模板阶段产生虚假节点事件（`tasks+values` 双 streamMode、真实节点 enter<exit 顺序、reviewer exit 在 interrupt 前、checkpoint 历史链/time-travel 已由 `smokeSummonResumeTimeTravel` 覆盖，单一有状态图 + 严格通道隔离（summon 不混入 taskActivity）由 `smokePlaybookExtensionNoRegression` 覆盖；本次新增 5.7 关键断言——在 `smokeWorkflowContinuationResume` 的真实工作流上下文（带 workflowRef 的 draft-writing 阶段运行）断言 graph-node-activated 事件均来自真实图节点集（supervisor/writer/reviewer/awaitDecision 等）、且不含任何模板阶段专有 id（concept/draft-writing/fact-extraction/chapter-plan 等）——证实模板阶段/stage-run 记录/asset-clarification 等工作流态操作不伪造虚假图节点事件。）

## 6. 新书创作工作流接入

- [x] 6.1 [Main] 将 concept-generator、worldbuilding、character-generator、architect、scene-outliner 全部接入 change-set candidate → 作者确认 → Main 提交链路，禁止直接 ingest 或写 CreativeAsset/Story Bible；仅将确认后的适用约束同步 Story Bible；researcher 仅持久化带来源/版本的 research artifact；无章节锚点的项目资产仍必须持久化（已实现：graph 的规划专家回调不再复用正文抽取/ingest 管线；Main runtime 按专家与模板阶段创建稳定 CreativeAsset 基线并只生成 pending candidate，下发 `creative-asset-change-proposed`，确认仍复用既有 workflow asset command；character/worldbuilding 的确认才进入 Story Bible；researcher 写入独立 `research_artifacts` 表并保存 source/sourceVersion；无 workflow/章节锚点时安全降级，项目级资产仍可持久化。workflow integration smoke 已覆盖映射、候选隔离、Story Bible 条件同步和 research artifact 隔离。）
- [x] 6.2 [Main] 实现章节/分场规划、正文写作、事实抽取、自动审校和人工修改/验收的 chapter scope 推进；将审校 finding 建立/去重为 `WorkflowIssueRecord` 并关联来源 run 与稳定正文锚点（已实现：章节/分场规划沿 chapter anchor 产候选资产，writer/scene-generator 的章节事实抽取沿既有 `afterWriterDraft` 管线执行；本次补齐新书 `writer + draft-writing + 稳定 chapter node` 的正文落盘，只在图正常完成且自动审校无 finding 时写入 Markdown，审校挂起绝不写盘，旧书/editor 仍严格走局部 diff/hunk 写回；writer 内部自动 reviewer 的 finding 现与直接审校一致，经 `upsertFromAudit(workflowId, runId, issues)` 建立/指纹去重 `WorkflowIssueRecord`，保留来源 run 与 canonical chapter/scene 锚点。orchestration smoke 覆盖规划阶段推进、finding 建 issue 且阻止写盘、无 finding 写入稳定 chapter node，以及既有事实抽取/人工 hunk 验收回归。）
- [x] 6.3 [Main/utilityProcess] 将事实抽取结果、冲突裁决和审校结果映射为质量门证据；CPU 密集处理继续在既有 worker 边界（已实现：事实抽取 pipeline 返回 `factVersion/autoIngested/conflicts/skipped` 并随 ActiveRun 合并进 stage-run interrupted/completed evidence；冲突裁决 accept-new/keep-existing 后更新最终 factVersion 与处理计数，补抽恢复/完成继续携带证据；全书审校将 `auditRunId/factVersion`、passed 与稳定 issueIds 一并写入质量门 evidence。CPU 密集 audit/diff 继续由 Main 注入的 UtilityProcessAuditRunner/UtilityProcessDiffRunner 承担，不改变既有 worker 边界；orchestration smoke 覆盖事实底稿与全书审校 evidence。）
- [x] 6.4 [Main] 实现章节定稿后的“下一章/结束创作”作者决策，创建新 chapter scope 循环或进入全书总检（已实现：`chapter-finalization` 的 `continue-loop` 必须携下一章稳定 `chapterId`，Core 追加实例化 chapter-plan→chapter-finalization 全套新 chapter scope 阶段，生成唯一 stageId 并清空 run/artifact/completion evidence，旧章节阶段完整保留且不被重置；Repository 在同一事务中 insert 新阶段并更新 currentStageId，confirm-stage 的 workflow-scope operationId 重放直接返回既有快照、不重复建 loop；`finish-loop` 显式进入 project scope `whole-book-audit`，默认 completed 路径保持兼容。workflow integration smoke 覆盖缺 chapterId 拒绝、新旧章节隔离、第二章推进不污染第一章、重放幂等及结束创作不创建新 loop。）
- [x] 6.5 [Main] 实现新书章节问题修复证据链：问题选择后进入 fixing，实际改写经 diff/逐 hunk 裁决与 checkpoint 落盘后仅转 verifying；针对性复检成功才 resolved，失败返回 fixing 并保留全部证据（已实现：新书 issue 选择经既有 workflow command 进入 fixing；`applyHunkDecisions` 将所有 hunk 规范化为 accept/reject 后仅局部写回，实际应用 run、完整裁决、accepted hunk、稳定 anchor、前后 hash 与 durable checkpoint 持久化到 migration v11 的 `workflow_issue_refactor_applies`，并与 fixing→verifying/issue-checkpoint 关联在同一 SQLite 事务完成；checkpoint/证据失败会按改写后片段范围补偿恢复原文。针对性复检仍是唯一 resolved 入口，失败回 fixing；resolution history 修复为保留真实 quality-gate run/evidence。orchestration smoke 已覆盖新书真实 diff/hunk/checkpoint 链、仅进 verifying、成功关闭、失败回环，以及 Repository 重载后两轮 apply/checkpoint/verification 证据完整保留。）
- [x] 6.6 [Main] 在章节定稿门校验所有 WorkflowIssueRecord 均为 resolved 或由作者带理由 dismissed；未解决问题 MUST 阻止定稿（已实现：`chapter-finalization`/全书审校门改用显式白名单查询 `countFinalizationBlocking`，仅 `resolved` 或 `dismissed + 非空 resolution_reason` 放行，open/fixing/verifying 及无理由 dismissed 均计为 blocking；dismiss 核心状态机拒绝空白理由，持久化保留 author actor、理由与 resolution history。workflow integration smoke 覆盖未解决问题阻断定稿、空白理由拒绝且 issue 保持 open、作者带理由 dismiss 经 Repository 重载仍保真并解除阻断，以及 resolved 正常放行。）
- [x] 6.7 [Integration Test] 覆盖“人物设计初稿 → 多轮人工意见 → 资产确认/Story Bible 同步 → 下一阶段”和“一章写作 → 抽取 → 审校建立 issue → diff/hunk → checkpoint → verifying → 复检失败回环/成功关闭 → 定稿 → 下一章”主路径（已实现：新增同一 `new-book-creation` workflow 的端到端 orchestration smoke。人物链由真实 `character-generator` 生成结构化初稿 pending candidate，后续两轮作者意见经 Main `workflow-change-asset` 形成候选，确认前 baseline/Story Bible 均不变；最终候选只经 Main `workflow-confirm-asset-change` 提交，验证资产版本与 workflowRef 事件，并原子同步人物名称、别名、最终属性到 Story Bible，再由作者确认进入 `book-outline`。章节链在同一 workflow/chapter 中由 writer 完成正文生成、自动事实抽取与审校建稳定 issue，随后推进事实抽取/自动审校/人工验收；同一 issue 连续执行两轮真实 diff/全 hunk 裁决/durable checkpoint，第一次针对性复检失败回 fixing，第二次成功 resolved，Repository 重载后两轮 apply/checkpoint/verification 证据仍完整；最后通过定稿问题门并以 `continue-loop + chapterId` 创建证据为空、scope 隔离的下一章阶段，第一章历史与 resolved issue 保留。另补齐 production 规划候选结构化 JSON 字段及项目级 provenance source，使真实 character/worldbuilding 输出具备确认后同步 Story Bible 的条件。）
- [x] 6.8 [Integration Test] 覆盖正文写作中随时澄清人物/世界观：目标资产消歧、change set 确认、新版本、影响清单、主阶段保持及 blocking/non-blocking 影响分流（已实现：跨阶段 `asset-clarification` 新增可选 `targetAssetId` 强类型 IPC/Renderer/Main 透传；Main 按 workflow project + asset kind 查询真实资产，同类目标不唯一时下发 `asset-target-selection-required` 候选清单并在模型运行/change set 前返回，禁止猜测及副作用；唯一目标可自动锁定，作者显式目标必须校验项目与类型归属。Runtime 将锁定的长生命周期 assetId 绑定到规划专家候选，仍跳过当前 stage-run，保持 `currentStageId`/StageStatus 不变。Main 确认沿既有字段级 change set/baseVersion 乐观锁创建新资产版本并保留历史，基于显式 dependency 生成 stale/needs-review/conflicting 影响事件与持久清单；受影响 stage 的 `conflicting` 阻断 start/confirm，作者处理后降级为剩余最高影响，stale/needs-review 作为 non-blocking 横切提示不改变阶段生命周期。另修复 workflow IPC schema 接受既有 `candidateId/chapterId`。新增同一 new-book workflow 的 orchestration smoke 覆盖双人物消歧零副作用、显式选中林岚、change set operations、确认前资产不变、v1/v2 与 applied change set 保留、stale+conflicting 清单、主写作阶段保持、冲突阻断及解除后 stale 放行。）

## 7. 老书问题修订闭环

- [ ] 7.1 [Main/utilityProcess] 将项目导入、全书事实回填与首次全书总检接入老书模板，并记录 factVersion 与 audit run 引用
- [ ] 7.2 [Main] 将总检 `ConsistencyIssue` 映射/去重为持久化 workflow issue records，并支持按 severity/status 选择修订项
- [ ] 7.3 [Main] 将 WorkflowIssueRecord 的 discovery/audit/history、问题定位、diff/hunk/checkpoint/verifying/复检回环与稳定 issue/chapter/node 锚点关联，并支持 resolved 后 reopen；锚点失效时阻塞而非猜测写入
- [ ] 7.4 [Main] 在 `refactor-applied` 事务结果中关联 accepted hunks、checkpoint 与 issue，并仅将问题推进到 verifying
- [ ] 7.5 [Main] 按问题类型/锚点/影响范围选择 reviewer、fact-checker 或 plagiarism-checker 执行针对性复检，并据结构化结果转 resolved 或返回 fixing
- [ ] 7.6 [Main/utilityProcess] 在队列均 resolved/dismissed 后运行最终全书复检；新问题或复发问题重新进入队列，通过且作者确认后完成工作流
- [ ] 7.7 [Integration Test] 覆盖“总检 discovery/audit → 选择问题 → 定位 → 改写 → diff/hunk → checkpoint → verifying → 复检失败回环/成功关闭 → resolved reopen → 最终总检”及全 lifecycle history 审计

## 8. 审校卡片、改写面板与质量仪表盘

- [x] 8.1 [Renderer] 在审校问题卡片显示 open/fixing/verifying/resolved/dismissed、下一动作及 dismissed 理由，保持原有严重度与锚点连线
- [x] 8.2 [Renderer] 从问题卡片发起修复时保留 workflow/stage/issue 与章节锚点，切换并等待目标章节加载；无锚点时禁用写入入口
- [x] 8.3 [Renderer] 将 `suggestedFix` 作为只读修改建议展示，改写正文输入保持独立且为空，只有实际 rewritten text 才可计算 diff
- [x] 8.4 [Renderer] 在 hunk 落盘后按后端快照显示 checkpoint 与 verifying，全部拒绝时不伪造修复状态
- [x] 8.5 [Renderer] 扩展质量仪表盘的 lifecycle/status 筛选、来源审计、checkpoint/复检追溯与最终复检新问题/复发提示
- [x] 8.6 [Renderer] 新增资产目标选择、字段级 change set 确认/拒绝、版本冲突和影响清单 UI；只上报意图，不在本地提交资产或判定影响
- [ ] 8.7 [Renderer Test] 覆盖建议与正文分离、跨章节修复、资产澄清消歧/确认、锚点缺失、待复检、已解决/已忽略区分及 Renderer 不本地关闭问题/写资产
- [ ] 8.8 [Renderer Polish] 恢复问题卡片到正文的连线 hero 动效；不改变定位、高亮和局部改写语义

## 9. 专家工作台双层视图

- [x] 9.1 [Renderer] 新增工作流快照 hook/store，按项目查询并消费 `workflow-snapshot-updated`，重载时不依赖本地事件重放
- [x] 9.2 [Renderer] 在 `ExpertWorkbench` 上层呈现工作流类型/目标、阶段列表、actor、状态、进度、阻塞原因、下一步与允许动作，并将横切资产澄清/影响提醒与主阶段分开展示
- [x] 9.3 [Renderer] 保留并复用现有 `WorkbenchGraph` / `useWorkbenchActivities` 作为下层当前阶段真实 run 轨迹，不改写节点事件语义
- [x] 9.4 [Renderer] 确保新 run 只重置下层轨迹，上层跨 run 历史不清空；standalone run 继续显示既有单次视图
- [x] 9.5 [Renderer] 调整折叠摘要为“工作流 + 当前阶段 + 下一步/阻塞”，并保证工具抽屉与三轴布局职责不回退
- [ ] 9.6 [Renderer Test] 覆盖人物设计多 run、写作中资产澄清但主阶段不变、资产影响阻塞/待办、暂停/等待确认/失败、章节循环、issue 循环、折叠摘要及 standalone 兼容

## 10. 人性化产品外壳与专注模式

- [x] 10.1 [Renderer IA] 将现有纵向工作流任务卡重构为产品栏、书目面包屑、业务 Workflow Graph、三栏主体与底部实时状态栏；移除正文上方常驻的目标编辑、内部 stage 标签和抽取技术指标堆叠
- [x] 10.2 [Renderer] 新增“设定目标”弹层，编辑 objective 与可重复的 preserve/extract/remove 要求；支持同类多条、增删改、空清单、并发冲突和保存后重新诊断提示
- [x] 10.3 [Main/IPC/Test] 完成 objective/author intents 更新命令及 optimistic version、operation id 幂等、阶段不变、旧诊断历史保留、下次诊断消费最新版的集成覆盖
- [x] 10.4 [Renderer] 实现面向作者的 Workflow Graph；阶段状态来自 workflow snapshot，当前章节/进度/发现摘要来自真实运行事件，内部 actor/id/version/impact 值不进入主视图
- [x] 10.5 [Renderer] 将事实抽取常驻面板迁移为底部实时摘要 + 按需“事实底稿”面板；支持从 Graph、底部进展和看板入口打开并定位当前章节/冲突，技术抽取指标默认折叠
- [x] 10.6 [Renderer] 将显式“召唤专家”入口放入对话区，将快捷键提示移至底部角落；顶部移除召唤提示，保留 `@专家` 与既有召唤 IPC 语义
- [x] 10.7 [Renderer] 引入单一判别 `ViewMode`，实现互斥 `workbench` / `reading` / `conversation` 模式，模式切换保留当前章节、阅读位置、专家上下文且不中断后台运行
- [x] 10.8 [Renderer] 实现读书模式：全屏人类阅读排版、书名/章节上下文、上一章/下一章、返回工作台、极简后台状态；隐藏 Graph、导航、对话、按需面板与 Hero 连线
- [x] 10.9 [Renderer] 实现对话专注模式：全屏对话、当前专家/任务/章节上下文、召唤/切换专家、返回工作台；隐藏正文、导航、Graph 与 Hero 连线
- [x] 10.10 [Renderer] 将 Hero 连线约束为非全屏工作台专属；模式切换时停止无意义渲染/坐标计算，返回工作台后仅在锚点仍有效时恢复
- [ ] 10.11 [Renderer Test/Manual] 验证窗口窄宽适配、键盘/hover/focus 可访问性、三种模式互斥、后台任务跨模式继续、冲突不强制退出读书模式、底部实时章节更新和事实底稿按需打开
- [x] 10.12 [Core/Main] 定义 `ModelTask`、`ModelTaskAttempt`、`ModelTaskActivity`、任务类型、活动阶段、任务作用域和结构化冲突裁决 DTO；明确任务会话与专家对话的边界
- [x] 10.13 [IPC] 定义模型任务活动、任务消息、失败、完成、作者补充、重试和中断的判别联合与命令契约；要求 task/run/workflow/stage/chapter scope、request metadata 和幂等性
- [x] 10.14 [Main] 将事实抽取 run 映射到独立模型任务会话，发布结构化活动和结果摘要；不发布隐藏思维链，不将任务消息写入专家聊天历史
- [x] 10.15 [Renderer] 在事实底稿/任务面板中按需展示模型任务活动、当前章节、证据、结果和冲突，不新增常驻第四栏
- [x] 10.16 [Renderer/Main] 支持任务会话中的受控作者补充要求和作用域选择，默认作用域为当前章节；不得将临时补充自动写入长期书目整理目标
- [x] 10.17 [Main/Test] 实现并验证任务重试保留旧 attempt、补充要求创建可追踪新尝试、confirmed 事实不可被自由文本覆盖、冲突只能通过结构化裁决落库

## 11. 迁移、回归与验收

- [ ] 11.1 [All] 更新相关文档与 smoke fixture，记录 `workflowId`/`runId` 语义、两套模板、人工门和 standalone 兼容路径
- [ ] 11.2 [Core/Main] 运行 node TypeScript、ESLint、领域/SQLite/IPC/LangGraph 单元与集成测试
- [ ] 11.3 [Renderer] 运行 web TypeScript、ESLint、组件测试和 Electron build
- [x] 11.4 [Smoke] 扩展并运行 orchestration smoke，验证阶段允许专家、跨 run 聚合、continuation 路由与既有单次召唤
- [ ] 11.5 [E2E] 完整验收新书主路径、任意阶段资产澄清链路和老书修订主路径，确认策划信息落入对应版本化资产、约束事实可追溯、所有正文写入均经过局部 diff + 逐 hunk，Renderer 无 DB/LLM/fs 访问
- [x] 11.6 [OpenSpec] 运行 `npx openspec validate workflow-guided-workbench --strict`，并在所有实现与验证完成前保持 change 未归档
