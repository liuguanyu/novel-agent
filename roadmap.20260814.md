# Novel Agent 老书整理 Roadmap（2026-08-14）

> 状态：讨论结论整理与后续行动基线
> 适用范围：已有正文的老书整理、全书大改与稳定后的局部修订
> 核心原则：**大改先重建全局认知，再生成新版大纲；小修只更新受影响的局部，不反复重跑全书。**

## 1. 当前判断

当前版本已经具备可真实试用的 Beta 能力：能够读取旧稿、还原并编辑情节候选、标记保留内容、查看情节地图、调用情节参谋、执行全书诊断，并把整理结果带入后续改写上下文。

但当前实现仍处于两个产品方向的过渡期：

1. 已有工作流偏向“建立底稿 → 全书诊断 → 逐问题修复 → 最终复检”；
2. 老书整理 v2 的目标应转向“还原旧稿 → 提炼故事资产 → 生成并确认新版大纲 → 按大纲调整正文 → 通读复检”。

因此，接下来不应继续把“问题清零”当作主线，而应让诊断、事实库和局部修复成为**大纲重建流程的辅助能力**。

---

## 2. 已确认的产品规则

### 2.1 全书大改

当作者要调整全书结构、核心冲突、主要人物动机、人物关系、成长弧或情节线时，执行完整链路：

```text
已有素材
→ 旧稿结构还原
→ 情节线提炼
→ 人物提炼
→ 当前故事资产
→ 新版大纲生成与人工确认
→ 用新版大纲反推人物/情节资产
→ 按新版大纲调整正文
→ 全书复检
```

全书大改必须重新梳理全局逻辑并生成新版大纲，避免在旧结构上连续打补丁。

### 2.2 稳定后的局部小修

当全书结构已经稳定，仅修改某个章节、段落、表达、局部情节或单个人物表现时：

- 不重新生成全书大纲；
- 直接进行局部重写或润色；
- 只更新受影响的情节节点、人物及其关系；
- 仅在人物动机、关系、成长弧或跨章因果发生变化时，触发局部影响分析和对应资产回填；
- 若局部修改扩散为全局结构变化，再升级为“全书大改”。

### 2.3 大改与小修的判定

满足任一条件时，建议升级为全书大改：

- 主线目标或结局改变；
- 核心人物动机、身份、关系或成长弧改变；
- 多条情节线的先后、因果或汇合方式改变；
- 大量章节需要移动、合并、删除或重写；
- 局部修改已影响多个卷或全书事实基线。

其余情况默认按局部小修处理。

---

## 3. 故事资产模型

大纲生成不应直接从整本正文临时总结后一次性输出，而应先形成可复用、可追溯、可回填的“当前故事资产”。

### 3.1 情节线资产

至少包括：

- 主线、支线及其目标；
- 关键事件及前因后果；
- 事件顺序、时间锚点和跨章连接；
- 伏笔的埋设、推进、回收状态；
- 每条情节线涉及的人物；
- 情节线的起点、转折、高潮和收束；
- 来源章节和原文证据；
- 作者明确要求保留的情节与原文。

### 3.2 人物资产

至少包括：

- 身份、经历、外貌、能力和知识边界；
- 核心性格、外在特点、语言与行为习惯；
- 欲望、目标、恐惧、弱点和底线；
- 主要关系、冲突和关系变化；
- 已发生的重要事件及人物当前状态；
- 人物成长弧及关键转折；
- 所属情节线与承担的叙事功能；
- 来源章节和原文证据。

### 3.3 信息可信度

每条提炼结果必须区分：

1. **原文明确**：正文或已有设定直接说明；
2. **合理推断**：根据人物言行、事件和上下文归纳；
3. **待作者确认**：存在冲突、歧义或多种解释；
4. **待补充设计**：现有素材不足，新版大纲需要补齐。

任何推断都应保留证据来源，不能悄悄升级为既定事实。

---

## 4. 大纲生成与反推闭环

### 4.1 生成前：从已有内容提炼

输入优先级：

1. 作者明确要求与裁决；
2. 已确认的人物、世界观和事实资产；
3. 保留情节与保留原文；
4. 旧版大纲；
5. 已有正文；
6. 模型合理推断和待补充设计。

处理步骤：

```text
读取已有素材
→ 识别章节情节候选
→ 合并跨章事件并形成情节线
→ 识别人物及关系
→ 汇总人物性格、动机、状态和成长方向
→ 标注证据、推断、冲突和缺口
→ 形成本轮“当前故事资产快照”
```

大纲生成节点只读取已经整理过的故事资产及必要原文证据，不一边生成大纲一边无边界地重新理解整本书。

### 4.2 生成中：以资产和保留约束生成新版大纲

新版大纲需要明确：

- 每个节点的目标、冲突、结果和下一节点因果；
- 所属情节线及其阶段；
- 参与人物、人物动机和状态变化；
- 对应旧稿来源；
- 对保留情节和保留原文的承接位置；
- 沿用、调整、合并、新增或删除的来源关系；
- 仍需作者裁决的问题。

生成结果必须先进入人工审阅，不直接覆盖现有大纲或正文。

### 4.3 生成后：由新版大纲反推故事资产

新版大纲确认后，执行反向回填：

```text
新版大纲
→ 重新计算情节线走向和节点归属
→ 回填人物目标、关系、状态与成长弧
→ 更新伏笔和事实状态
→ 标记与旧稿资产的差异
→ 生成正文调整影响范围
```

反推不是无条件覆盖。出现以下情况必须让作者裁决：

- 新大纲与原文明确事实冲突；
- 人物行为与既有人设明显不一致；
- 保留内容在新大纲中无承接位置；
- 同一人物或事件存在多个互斥解释；
- 推断信息准备转为正式设定。

### 4.4 正文调整后的再次回填

正文改写被作者接受后，才把实际发生的变化回填至人物、情节线、事实库和大纲状态。大纲草案或未采纳改写不得污染当前正式资产。

---

## 5. 推荐产品路径

### 阶段 A：旧稿梳理

目标：看清旧稿已经写了什么。

- 读取章节树与正文；
- 还原卷、章和章节内情节；
- 编辑、删除、恢复、排序情节候选；
- 合并跨章事件或记录跨章关联；
- 标记保留情节和保留原文；
- 形成旧稿大纲和来源证据。

### 阶段 B：故事资产提炼

目标：从旧稿中提炼可供大纲生成使用的结构化资产。

- 提炼主线、支线、伏笔和因果；
- 提炼人物性格、动机、关系、状态和成长弧；
- 标注“原文明确 / 合理推断 / 待作者确认 / 待补充设计”；
- 处理冲突与缺口；
- 由作者确认关键人物和关键情节线。

### 阶段 C：新版大纲

目标：在保留约束和当前故事资产基础上形成新版结构。

- 生成新版大纲草案；
- 展示与旧稿大纲的映射和差异；
- 展示每个保留项的承接位置；
- 支持逐节点编辑、移动、合并、新增和删除；
- 人工确认后形成正式新版大纲版本；
- 反推并更新人物、情节线和影响范围。

### 阶段 D：正文调整

目标：按新版大纲有控制地修改正文。

- 从新版大纲节点进入调整；
- 自动装配相关原文、保留内容、人物资产、情节线和事实；
- 生成局部改写草案；
- 展示差异，逐段接受或拒绝；
- 通过 checkpoint 写入修订稿，不覆盖原稿；
- 已接受变更回填故事资产。

### 阶段 E：通读与复检

目标：确认修订稿作为完整作品是否成立。

- 检查人物一致性、时间线、因果、伏笔和保留约束；
- 明确反馈“发现 N 个问题 / 未发现问题 / 失败原因”；
- 问题进入建议列表，不替代大纲主线；
- 作者可局部修复，也可在结构性问题出现时回到新版大纲阶段。

---

## 6. 近期修复路线（稳定性与 UX）

### P0：真实可用性

1. **参谋输出截断**
   - 已将结构化输出预算提高到 8192 tokens；
   - 用《津门余味》的长章节和连续追问再次执行真实模型测试；
   - 若仍截断，增加上下文摘要、输出重试/修复解析或分段建议，而不是仅提示重试；
   - 成功标准：连续多轮真实调用均返回可解析建议，无数据污染。

2. **诊断结束反馈**
   - 当前已支持“发现 N 个候选 / 暂未发现 / 失败原因”；
   - 补充真实模型回归，确认成功、空结果、失败三条路径均能收敛；
   - 该项完成后转为回归测试，不再作为开放缺陷。

3. **数据安全**
   - 所有模型候选先进入草案态；
   - 只有作者采纳后才能成为正式问题、正式大纲或正式资产；
   - 所有正文写入均通过 checkpoint，原稿保持不变；
   - 为真实模型 E2E 固化备份与恢复检查。

### P1：操作反馈与入口收敛

1. 无情节候选时只显示一个主按钮，合并“重新识别”和“识别本章情节”；
2. 长任务显示真实阶段，例如读取章节、分片识别、章节归并、全书合并、诊断收敛；
3. 有可靠统计时显示 `已处理/总数`，没有可靠进度时不伪造百分比；
4. 为可中断任务提供取消入口，并明确取消后不会修改数据；
5. 对失败状态提供具体原因和原位重试；
6. 用 Electron 长时间运行复测 `MaxListenersExceededWarning`，确认订阅在卸载时清理；必要时改为单一事件总线订阅后在 Renderer 内分发。

### P2：信息架构

1. 将老书主路径收敛为：

   ```text
   旧稿梳理 → 故事资产 → 新版大纲 → 正文调整 → 通读
   ```

2. 将诊断和问题处理降为辅助入口；
3. 在每个阶段明确展示：当前目标、当前产物、作者要做什么、系统不会修改什么；
4. 三栏旧稿页面保留专业效率，但为首次使用提供渐进式提示和空状态主动作；
5. 区分“旧稿大纲”“新版大纲”“修订稿”，避免用户误认为当前操作已经改动原稿。

---

## 7. 实施里程碑

### M0：当前 Beta 收口

- 提交并推送现有旧稿大纲、保留清单、情节地图、情节参谋、全书诊断和改写上下文能力；
- 保持类型检查、构建及老书 Smoke 全绿；
- 完成 P0/P1 的真实模型与 Electron 实机回归；
- 输出一份可重复执行的验收记录。

### M1：故事资产数据层

- 定义 `PlotThread`、`CharacterProfile`、`CharacterRelation`、`CharacterArc` 等资产结构；
- 每条结论保存来源证据、可信度和作者确认状态；
- 支持资产版本、草案与正式状态；
- 建立人物—情节—章节—事实之间的可追溯关联；
- 增加提炼、合并、冲突检测和持久化测试。

### M2：提炼工作流

- 完成章节情节 → 跨章情节线的聚合；
- 完成人物识别、别名合并、性格/动机/关系/状态提炼；
- 支持只重提炼受影响人物或情节线；
- 提供作者确认、修正和补充入口；
- 输出“当前故事资产快照”。

### M3：新版大纲生成

- 以当前故事资产、保留约束和作者要求生成新版大纲草案；
- 展示旧稿到新版的节点映射；
- 展示保留项覆盖情况和遗漏警告；
- 支持人工编辑和版本确认；
- 新版大纲确认后执行人物、情节线和影响范围反推。

### M4：按大纲调整正文

- 以新版大纲节点为正文调整单元；
- 装配人物、情节线、事实和保留约束；
- 逐段差异审阅、checkpoint 写入与撤销；
- 已接受修改增量回填正式故事资产；
- 支持稳定期的局部重写/润色快捷路径。

### M5：通读、复检与发布准备

- 全书人物、时间线、因果、伏笔及保留内容复检；
- 结构性问题可回退大纲阶段，局部问题直接进入局部修订；
- 完成长时间运行、失败恢复、重连和数据迁移测试；
- 以普通作者独立完成整本旧稿整理作为 Beta 退出标准。

---

## 8. 验收标准

### 8.1 全书大改闭环

- 能从已有正文生成带来源证据的旧稿大纲；
- 能提炼并人工确认主要情节线和主要人物；
- 能区分明确事实、合理推断、待确认和待设计；
- 能基于故事资产与保留约束生成新版大纲；
- 能检查所有保留项是否被新版大纲承接；
- 能由新版大纲反推人物与情节线变化，冲突时要求作者裁决；
- 能按新版大纲调整正文且不覆盖原稿；
- 能在接受正文变更后回填正式资产并完成全书复检。

### 8.2 局部小修闭环

- 作者可直接选择章节、片段或大纲节点进行重写/润色；
- 不重新生成全书大纲；
- 系统能计算受影响人物、情节线、事实和后续章节；
- 只更新受影响资产；
- 影响扩大到全局时，明确建议切换到全书大改流程。

### 8.3 UX 与稳定性

- 所有长任务都有开始、进行中、成功、空结果和失败反馈；
- 可中断任务提供取消与恢复说明；
- 不出现重复主入口；
- 长时间使用不再出现监听器泄漏警告；
- 真实模型失败不会留下半成品正式数据；
- 普通作者无需开发者解释即可判断当前处于哪一步、下一步做什么。

---

## 9. 下一轮建议执行顺序

1. 真实模型复测参谋截断、诊断三种结束状态；
2. Electron 实机检查监听器警告；
3. 合并情节识别重复按钮，补长任务进度和取消；
4. 冻结故事资产最小数据模型；
5. 先实现“情节线提炼 + 人物提炼 + 证据/可信度”；
6. 再实现“当前故事资产 → 新版大纲 → 反向回填”；
7. 最后把正文调整入口从问题驱动迁移到新版大纲节点驱动；
8. 全链路跑《津门余味》真实验收，并保留可恢复的数据快照。

这条路线的关键不是再增加一个模型按钮，而是把**旧稿、故事资产、新版大纲和修订稿**变成边界清楚、可追溯、可人工裁决的四类正式对象。只有这样，全书大改和后续局部小修才能共用一套稳定底座，而不会每次都从头理解整本书。

---

## 10. 技术栈结论与架构约束

### 10.1 总体结论

当前技术栈选择与产品方向匹配，**不需要换栈或重写**。现有底座能够支持“本地桌面端 + 长篇文本编辑 + 多模型编排 + 人工审阅 + 本地持久化”的核心需求。

真正需要控制的不是技术选型本身，而是随着人物、情节线、大纲反推和长任务增加后出现的架构膨胀。

### 10.2 现有选型判断

- **Electron + React + TypeScript**：适合访问本地书稿、构建复杂桌面工作台和多栏编辑界面；继续沿用。
- **TipTap**：能够支撑正文编辑、选区、批注、局部改写和后续结构化标记；继续沿用。
- **LangGraph / LangChain**：适合多阶段模型任务、人工中断与恢复、条件分支和长流程编排；继续沿用，但业务状态不能只存在于图运行内。
- **Zod + 强类型 IPC**：适合约束 Renderer、Main 与模型结果之间的数据边界；新增领域对象和事件必须继续保持强类型。
- **本地持久化 + checkpoint**：符合原稿安全、版本恢复、离线优先和人工确认后落盘的要求；继续作为数据安全底座。
- **ModelAdapter / ModelResolver 抽象**：能够按任务选择模型并降低供应商绑定；人物提炼、大纲生成和复检都应复用该抽象。

### 10.3 必须守住的架构约束

1. **Main 进程不演化成巨型业务层**
   - IPC Handler 只负责输入校验、权限/项目边界、命令转发和事件回传；
   - 人物提炼、情节线聚合、大纲生成、反推和影响分析进入独立领域服务或任务执行器；
   - 禁止把复杂业务继续直接堆入 `ipc-handlers.ts`。

2. **长模型任务统一任务化**
   - 任务需要持久状态、真实阶段、可取消、失败恢复、幂等和重连查询；
   - IPC 不应承担从请求发出到模型完成的整段长等待；
   - 不虚构百分比，只呈现能够从实际工作量计算出的进度。

3. **故事资产结构化、版本化**
   - 人物、关系、成长弧、情节线和新版大纲不能只存在于 prompt 或临时 JSON 中；
   - 明确草案、作者确认版和正式版；
   - 每条提炼结论保存证据、可信度、来源版本和作者裁决；
   - 未采纳的大纲或正文草案不得污染正式资产。

4. **统一 Renderer 事件订阅**
   - 当前多个 Hook 分别监听 `control-event`，应逐步收敛为窗口级单一订阅；
   - 由 Renderer 内部 store 或事件总线按事件类型分发；
   - 以消除重复监听、遗漏退订和 `MaxListenersExceededWarning` 风险。

5. **稳定工具链，集中升级**
   - 当前 `electron-vite 6 beta + Vite 8` 已能通过生产构建，但存在弃用警告；
   - 当前阶段锁定依赖和 lockfile，不边开发核心业务边追逐工具链变化；
   - 待稳定版本成熟后单独安排升级、回归和构建产物核验。

### 10.4 当前不建议进行的技术迁移

- 不为追求更轻量而迁移到 Tauri；
- 不把编排层改写为 Python；
- 不切换到 Next.js；
- 不因局部复杂就更换编辑器或状态管理体系；
- 不把本地桌面应用过早拆成微服务。

这些迁移会增加工程成本，却不能解决当前最核心的问题：故事资产模型、大纲生成与反推闭环尚未完成。

### 10.5 技术工作优先级

```text
稳定现有桌面与任务底座
→ 拆分 Main 业务职责
→ 建立版本化故事资产
→ 统一长任务运行与事件分发
→ 完成人物/情节线/大纲闭环
→ 再集中处理工具链升级
```

最终判断：**地基没有选错，下一阶段应画清承重墙。** 技术投入优先用于领域边界、任务可靠性和数据可追溯性，而不是换框架。
