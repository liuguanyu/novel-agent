# Novel Agent 任务驱动创作工作台需求文档

> 状态：产品方案草案  
> 适用范围：旧作重建、新书创作、续写、修订与全书复检  
> 核心原则：用创作目标组织计划，用当前任务驱动工作台，用消息流证明系统仍在工作，用 UI Effect 将任务结果真实反馈到创作内容中。

## 1. 背景

当前工作台已经具备流程导航、事实底稿、模型任务、专家对话、正文工作区和底部任务状态等能力，但整体产品认知仍偏向“技术状态机/流程监视器”：

- 用户能看到阶段变化，却不一定知道当前具体要解决什么问题。
- 底部活动主要由前端根据状态快照拼装，不能完整表达任务的真实执行轨迹。
- 任务涉及原文时，活动记录与正文切章、滚动、高亮、Diff、checkpoint 等反馈尚未形成统一闭环。
- Workflow 阶段占据较强视觉权重，而作者真正关心的是当前任务、当前问题和下一步决策。
- 当前流程以旧书重建为主，但产品未来还需要支持从零创作新书、续写和日常修订，不能将底层模型绑定到“已有旧小说”。

因此，产品需要从“流程驱动的技术工作台”升级为“任务驱动的小说创作工作台”。

## 2. 产品定位

Novel Agent 是一个能够持续理解小说上下文、执行创作任务，并让作者始终看见它正在做什么的创作工作台。

产品不应围绕某一套固定 Workflow 设计，而应围绕“作者当前正在完成的创作任务”设计。旧作重建、新书创作、续写、修订和复检只是不同的计划模板，它们共享同一套任务运行、活动反馈、作者确认和产物管理能力。

产品的核心闭环为：

```text
创作目标
→ 创作计划
→ 当前任务
→ 工作活动流
→ 产物与工作区变化
→ 作者确认
→ 下一任务
```

## 3. 产品目标

### 3.1 用户认知目标

作者在任意时刻都应能够回答：

1. 系统现在在做什么？
2. 为什么要做这件事？
3. 当前要解决什么问题？
4. 系统使用了哪些输入？
5. 系统是查正文、查事实底稿、读取诊断、调用工具，还是调用模型？
6. 已经产生了什么结果？
7. 页面为什么发生了切换、高亮或内容变化？
8. 当前是否需要作者确认？
9. 下一步是什么？

### 3.2 产品能力目标

- 以“当前任务”作为页面主叙事，而不是以技术阶段作为主叙事。
- 保留流程导航，但将其降级为轻量的整体进度辅助。
- 建立真实、统一、可追溯的 Task Runtime 和 Task Activity Stream。
- 底部常驻消息流，让作者持续感知“产品还在工作”。
- 将任务活动与正文、大纲、诊断、Diff、checkpoint 等 UI 反馈统一起来。
- 模型交互可审计，但不展示模型隐藏的 chain-of-thought。
- 同一套任务模型支持旧作重建和新书创作，避免未来推翻架构。

## 4. 非目标

本阶段不以以下内容为目标：

- 不要求一次性完成所有旧作重建阶段和新书创作阶段的运行时接入。
- 不将底部消息流建设成面向开发者的 debug console。
- 不在主界面暴露 `stageId`、`runId`、`operationId`、actor ID 或内部流程标识。
- 不展示模型隐藏的推理过程或 chain-of-thought。
- 不强制所有项目使用同一套固定阶段；流程应来自可调整的计划模板。
- 不通过虚假百分比、循环文案或重复动画伪造任务进度。

## 5. 核心概念

### 5.1 创作项目

一本正在创作、续写、修订或重建的小说及其全部上下文，包括正文、人物、世界观、故事线、诊断结果、任务产物和历史决策。

### 5.2 创作目标

作者希望最终完成的结果，例如：

- 修复人物动机前后不一致。
- 保留主线，重建感情线。
- 完成第一卷大纲。
- 写出第三章初稿。
- 检查最近五章是否存在人设漂移。

### 5.3 创作计划

系统根据目标组织的一组阶段和任务。计划可以来自模板，也可以在执行中调整，不是不可变的技术状态机。

### 5.4 当前任务

当前正在执行或等待作者处理的最小业务任务，例如：

- 定位诊断问题对应的原文。
- 生成局部改写方案。
- 审核本次改写。
- 设计第三章场景。
- 生成章节初稿。
- 检查时间线连续性。

### 5.5 工作活动流

任务执行过程中产生的作者可读事件，用于说明任务输入、执行动作、阶段结果、工作区反馈和下一步。活动流不是从最终状态反推出来的展示文案，而应由真实任务运行过程发布。

### 5.6 任务产物

任务生成或更新的业务结果，例如定位结果、事实底稿、诊断报告、场景方案、章节初稿、Diff、checkpoint 和复检报告。

### 5.7 UI Effect

任务运行对工作台产生的可观察反馈，例如切换章节、滚动到证据、高亮原文、打开 Diff 或展示 checkpoint。

## 6. 整体产品形态

工作台采用稳定外壳和任务驱动内容区：

```text
产品栏
→ 轻量流程导航
→ 当前任务卡
→ 三栏工作区
→ 底部消息流
→ 可展开的任务中心
```

建议页面结构：

```text
┌────────────────────────────────────────────────────────────┐
│ Novel Agent / 《书名》 / 当前创作模式          目标  读书  对话 │
├────────────────────────────────────────────────────────────┤
│ 已完成 4/11 · 当前：定位原文 · 下一步：生成局部改写   [展开流程] │
├────────────────────────────────────────────────────────────┤
│ 当前任务：定位“顾长风动机不一致”对应的原文                   │
│ 问题 / 输入 / 执行方式 / 预期输出 / 当前进度                  │
│ [查看问题] [跳到原文] [重新定位] [暂停任务]                   │
├──────────────┬──────────────────────────┬──────────────────┤
│ 章节 / 问题   │ 正文 / 诊断 / Diff / 大纲 │ 当前任务助手       │
│              │                          │                  │
├──────────────┴──────────────────────────┴──────────────────┤
│ ● 已找到诊断证据，正在匹配第 7 章原文……       [查看任务中心] │
└────────────────────────────────────────────────────────────┘
```

## 7. 页面区域需求

### 7.1 产品栏

产品栏保持稳定，不随任务频繁变化，应提供：

- 产品名称。
- 当前书名。
- 当前创作模式，如旧作重建、新书创作、续写、修订、润色、全书复检。
- 创作目标入口。
- 读书模式入口。
- 专注对话入口。
- 主题与全局菜单。

“创作模式”只决定计划模板和推荐能力，不应限制底层任务类型。

### 7.2 轻量流程导航

流程导航用于回答“整体进行到哪里”，不能抢占当前任务的视觉主位。

默认仅展示：

```text
已完成 4/11 · 当前：定位原文 · 下一步：生成局部改写
```

用户主动展开后，才展示完整流程图、各阶段状态和历史结果。

流程应支持不同模板。例如新书创作可以展示：

```text
第一卷已完成 3/8 · 当前：设计第三章场景 · 下一步：生成章节初稿
```

### 7.3 当前任务卡

当前任务卡是工作台核心区域。每个任务必须说明：

1. 任务名称。
2. 当前问题或创作目标。
3. 任务目的。
4. 输入。
5. 执行方式。
6. 预期输出。
7. 当前状态和进度。
8. 作者当前可执行的主操作。
9. 下一步或等待作者的事项。

旧作定位任务示例：

```text
当前任务
定位“顾长风动机前后不一致”对应的原文

任务目的
找到可以进行修订的具体章节和段落。

输入
- 全书诊断问题
- 第 7 章与第 12 章证据
- 当前正文
- 人物事实底稿

执行方式
读取诊断证据 → 匹配章节锚点 → 搜索原文 → 验证上下文

预期输出
目标章节、候选段落、原文高亮、下一步改写入口
```

新书任务示例：

```text
当前任务
设计第三章“第一次正面冲突”的场景方案

任务目的
让主角第一次意识到对手并非普通竞争者。

输入
- 第一卷目标
- 主角与反派人物卡
- 前两章正文
- 已建立的世界观规则

执行方式
读取前文 → 检查人物状态 → 生成场景目标与冲突 → 验证谜底边界

预期输出
场景目标、节拍列表、出场人物、伏笔和章节结尾钩子
```

### 7.4 左栏：上下文导航

左栏围绕当前任务提供上下文入口，不固定为章节树。至少应支持以下视图按需切换：

- 章节
- 问题
- 人物
- 故事线
- 任务产物

默认视图由当前任务决定：

- 定位原文默认打开“问题”。
- 章节写作默认打开“章节”或“故事线”。
- 人物诊断默认打开“人物”。

用户选择问题、章节或人物后，中栏和当前任务上下文应同步响应。

### 7.5 中栏：主要工作面

中栏根据当前任务承载主要输入和产物，可以显示：

- 正文
- 大纲
- 诊断结果
- 人物卡
- 故事线
- 时间线
- Diff
- Hunk 审核
- checkpoint
- 任务反馈
- 失败、暂停或等待作者状态

中栏变化必须有明确的任务原因，并同步记录到消息流。例如：

- 已定位到第 7 章，工作区已切换到对应正文。
- 已生成改写方案，工作区已进入差异审核。
- 已完成正文落盘，工作区已展示本次 checkpoint。

中栏空状态也必须使用任务语言，不能只展示“未选择章节”。例如：

```text
正在定位原文
请选择左侧诊断问题。系统会根据问题证据查找对应章节，
并在正文中高亮候选段落。
```

### 7.6 右栏：当前任务助手

右栏应随当前任务切换角色，而不是固定为审核或通用聊天：

| 当前任务 | 助手角色 |
| --- | --- |
| 建立事实底稿 | 事实整理助手 |
| 全书诊断 | 故事诊断助手 |
| 定位原文 | 原文定位助手 |
| 生成改写 | 改写助手 |
| Hunk 审核 | 修订审核助手 |
| 新书大纲 | 故事策划助手 |
| 章节写作 | 章节创作助手 |
| 连贯性检查 | 连贯性审校助手 |

任务助手应提供：

- 当前任务简要说明。
- 推荐问题和推荐操作。
- 与当前任务上下文绑定的对话。
- 作者补充约束的入口。
- 作者补充内容被任务采用后的明确反馈。

作者新增约束后，必须成为当前任务的新输入，并进入活动流。例如：

```text
作者补充要求：本章不能明确揭露反派身份。
系统正在据此重新检查场景方案。
```

## 8. 底部消息流

### 8.1 产品角色

底部消息流是工作台常驻的“工作脉搏”，用于建立“产品仍在工作”的持续认知。消息流必须保留，不能仅依赖加载动画、阶段标题或抽屉中的历史日志。

### 8.2 默认展示

默认仅展示最近 2～3 条最有价值的活动，避免占用主要工作空间：

```text
● 正在匹配第 12 章原文
  已读取诊断证据，正在验证候选段落上下文

✓ 已定位第 7 章第一处候选内容
  已切换章节并高亮原文

[查看完整任务]
```

### 8.3 内容要求

每个活动应按适用情况包含：

- 正在执行的动作。
- 使用的输入或上下文。
- 已产生的输出。
- 对工作区产生的反馈。
- 失败或不确定原因。
- 下一步或等待作者的动作。

消息主文本必须使用作者可理解的业务语言，不得以内部 ID、事件类型或技术阶段代码作为主要表达。

### 8.4 长任务 heartbeat

运行中的任务超过 2 秒没有产生新活动时，必须发布真实 heartbeat。heartbeat 应至少反映下列一种真实状态：

- 已处理数量和总数量。
- 当前处理的章节、人物或问题。
- 当前执行步骤。
- 最近完成的子步骤。
- 正在等待的外部服务。

示例：

```text
正在检查全书 86 个章节，已完成 31 个。
仍在分析人物关系，已整理 14 位主要人物。
正在生成章节初稿，当前已完成场景主体。
```

禁止使用虚假百分比、无事实依据的进度或循环文案制造“正在工作”的假象。

## 9. 任务中心

点击底部消息流后打开任务中心。任务中心是完整任务历史和作者决策入口，不替代底部常驻消息流。

### 9.1 当前任务

展示：

- 任务目标和状态。
- 当前步骤和运行时间。
- 任务输入。
- 完整活动时间线。
- 已产生的产物。
- 暂停、继续或取消操作。

### 9.2 等待作者

聚合所有需要作者处理的事项，例如：

- 确认原文候选位置。
- 选择改写方向。
- 审核章节初稿。
- 接受或拒绝 hunk。
- 处理事实冲突。
- 决定是否关闭问题。

### 9.3 历史任务

历史任务至少支持按以下维度查找或过滤：

- 章节
- 人物
- 问题
- 任务类型
- 时间
- 任务状态
- 是否修改正文

任务中心中的完整活动示例：

```text
14:32:05 任务开始
定位“顾长风动机前后不一致”对应的原文

14:32:05 已接收输入
诊断问题 1 条、证据 2 条、相关人物 1 位

14:32:06 读取诊断结果
证据指向第 7 章和第 12 章

14:32:06 查找第 7 章原文
已匹配引用文本，置信度较高

14:32:07 更新工作区
已切换到第 7 章，并高亮目标段落

14:32:08 查找第 12 章原文
原引用与当前正文存在差异，正在进行近似匹配

14:32:09 等待作者
找到两个候选段落，请确认需要修改的位置
```

## 10. 模型交互与可审计性

模型参与任务时，应展示可审计的工作记录，但不得展示隐藏 chain-of-thought。

### 10.1 必须展示

- 调用模型的任务目标。
- 模型可见输入摘要。
- 使用的正文、人物卡、事实底稿、诊断或其他上下文。
- 作者约束和系统业务约束。
- 模型输出摘要。
- 结构化结果。
- 引用和证据来源。
- 工具调用结果。
- 输出验证结果。
- 哪些结果被采用、拒绝或等待确认。

### 10.2 禁止展示

- 模型隐藏的 chain-of-thought。
- 内部 prompt 中不适合作者理解的实现细节。
- 以“模型认为”包装但无法追溯来源的虚假解释。

### 10.3 示例

```text
调用创作模型：生成局部改写方案

任务
补足顾长风在决定离开宗门前的动机铺垫。

使用上下文
- 第 7 章目标段落
- 第 4 章人物承诺
- 第 12 章后续行动
- 人物事实底稿中的核心欲望与禁忌

约束
- 不改变离开宗门这一既定事件
- 不新增主要人物
- 保留原叙事视角
- 控制在 300 字以内

返回结果
生成 3 个改写方向：责任冲突、被迫隐瞒、对师门失望。

验证
三个方案均未改变既有事实；方案 2 与第 12 章已知信息存在轻微冲突。
```

## 11. Task UI Effect

任务活动不能只停留在日志中。任务涉及正文、诊断或创作产物时，必须对工作区产生可观察反馈。

至少支持：

- 选择章节。
- 滚动到证据。
- 高亮原文。
- 展示诊断标记。
- 打开大纲节点。
- 展示生成内容。
- 打开 Diff。
- 进入逐 hunk 审核。
- 展示 checkpoint。
- 打开事实底稿。
- 打开复检报告。

每个 UI Effect 必须同步进入活动流，例如：

```text
已切换到第 7 章。
已高亮诊断证据对应的原文。
已打开本次改写差异。
已创建正文 checkpoint。
```

消息流和工作区必须表达同一个任务事实：消息流说明发生了什么，工作区展示结果在哪里。

## 12. 旧作与新书共用模型

底层产品模型统一为：

```text
创作项目
→ 创作目标
→ 计划模板
→ 当前任务
→ 上下文
→ 活动流
→ 产物
→ 作者决策
```

### 12.1 旧作重建模板

```text
确认重建目标
→ 建立事实底稿
→ 全书诊断
→ 确定优先级
→ 定位原文
→ 生成改写
→ 逐段审核
→ 正文落盘
→ 针对性复检
→ 问题关闭
→ 全书复检
```

### 12.2 新书创作模板

```text
明确创作目标
→ 建立核心设定
→ 建立人物关系
→ 设计故事主线
→ 规划卷与章节
→ 设计当前章节
→ 生成章节初稿
→ 作者修订
→ 连贯性检查
→ 更新事实底稿
→ 推进下一章
```

### 12.3 续写模板

```text
读取已有正文
→ 恢复人物与故事状态
→ 识别未解决线索
→ 确定下一章目标
→ 生成场景计划
→ 生成章节
→ 检查连续性
→ 作者确认
→ 更新事实底稿
```

### 12.4 临时任务

作者应能在主计划之外直接发起临时任务，例如：

- 检查最近五章是否有人设漂移。
- 将某段改得更克制。
- 续写下一场戏，但不推进感情线。
- 将某个伏笔提前安排到前三章。
- 列出尚未回收的线索。

临时任务完成后，系统应保存其活动和产物，并允许返回主计划。

## 13. Task Runtime

### 13.1 职责

Main 侧应提供统一 Task Runtime，负责：

- 为当前任务创建 task run。
- 记录任务输入。
- 执行任务 playbook。
- 发布真实活动事件。
- 记录工具和模型交互摘要。
- 保存任务产物。
- 发布 UI Effect。
- 发布 heartbeat。
- 处理暂停、中断、失败和等待作者。
- 保存作者决策。

Renderer 不应长期依赖 workflow snapshot、dashboard 或其他最终状态拼装“伪运行日志”。现有拼装机制只可作为迁移期间的兼容方案。

### 13.2 任务状态

统一任务状态至少包括：

```text
queued
running
awaiting-author
paused
completed
failed
cancelled
```

### 13.3 活动事件

统一活动事件至少覆盖：

```text
task-run-started
task-run-input-declared
task-run-step-started
task-run-step-completed
task-run-tool-used
task-run-model-interaction
task-run-output-produced
task-run-ui-effect
task-run-awaiting-author
task-run-heartbeat
task-run-completed
task-run-failed
```

建议事件结构：

```ts
interface TaskActivityEvent {
  type: 'task-activity'
  taskRunId: string
  workflowRef?: WorkflowRefDto
  issueId?: string
  chapterId?: string

  phase:
    | 'input'
    | 'retrieval'
    | 'model'
    | 'validation'
    | 'ui-effect'
    | 'output'
    | 'awaiting-author'
    | 'completed'
    | 'failed'
    | 'heartbeat'

  title: string
  message: string

  inputSummary?: string
  outputSummary?: string
  feedback?: string
  nextAction?: string

  evidenceRefs?: ReadonlyArray<EvidenceRefDto>
  artifactRefs?: ReadonlyArray<ArtifactRefDto>
  uiEffects?: ReadonlyArray<TaskUiEffectDto>

  createdAt: string
}
```

建议 UI Effect 类型：

```ts
type TaskUiEffect =
  | { kind: 'select-chapter'; chapterId: string; reason: string }
  | { kind: 'highlight-quote'; chapterId: string; quote: string; reason: string }
  | { kind: 'scroll-to-evidence'; chapterId: string; quote: string }
  | { kind: 'show-diff'; nodeId: string; diffId: string }
  | { kind: 'show-hunk-review'; refactorRunId: string }
  | { kind: 'show-checkpoint'; checkpointId: string }
  | { kind: 'open-fact-sheet'; taskId: string }
  | { kind: 'open-dashboard'; auditRunId: string }
```

具体 DTO 应在 OpenSpec 和实现阶段结合现有 IPC 类型进一步约束。

## 14. Task Playbook

每种任务不能只有阶段名称，必须定义可解释、可执行的 playbook。

统一结构至少包括：

```ts
interface TaskPlaybook {
  purpose: string
  inputs: TaskInputDefinition[]
  executionPlan: TaskStepDefinition[]
  possibleToolCalls: ToolDefinition[]
  possibleModelInteractions: ModelInteractionDefinition[]
  outputs: TaskOutputDefinition[]
  uiEffects: TaskUiEffectDefinition[]
  authorDecisionPoints: AuthorDecisionDefinition[]
  failureModes: TaskFailureMode[]
  nextTaskSuggestions: NextTaskSuggestion[]
}
```

每个 playbook 必须定义：

- purpose：任务为什么存在。
- inputs：需要哪些显式输入和自动上下文。
- executionPlan：准备按什么业务步骤执行。
- possibleToolCalls：可能读取或调用哪些工具。
- possibleModelInteractions：模型何时参与以及输入输出要求。
- outputs：任务产物。
- uiEffects：工作区如何响应。
- authorDecisionPoints：何时必须等待作者。
- failureModes：无法完成时如何解释和恢复。
- nextTaskSuggestions：成功后的下一任务建议。

## 15. 首个端到端任务：定位原文

`locate-source` 是第一个完整接入 Task Runtime、Activity Stream 和 UI Effect 的验收任务。

### 15.1 任务目的

将抽象的诊断问题映射为可修订的具体章节和原文位置。

### 15.2 输入

- 当前诊断问题。
- 证据引用。
- 章节锚点。
- 当前正文。
- 相关人物或事实底稿。

### 15.3 执行计划

1. 接收并展示诊断问题。
2. 解析诊断证据指向。
3. 按章节锚点缩小范围。
4. 精确匹配引用文本。
5. 精确匹配失败时执行近似匹配。
6. 验证候选段落上下文。
7. 输出一个或多个候选位置。
8. 发布切章、滚动和高亮效果。
9. 候选不唯一时等待作者确认。
10. 确认后提供进入局部改写的下一步操作。

### 15.4 活动流示例

```text
开始定位“顾长风动机前后不一致”对应的原文。
已读取 2 条诊断证据，指向第 7 章和第 12 章。
正在匹配第 7 章原文。
已找到第 7 章候选段落，正在验证上下文。
已切换到第 7 章并高亮目标段落。
第 12 章引用与当前正文存在差异，正在近似匹配。
找到两个候选位置，请作者确认需要修改的段落。
```

### 15.5 UI Effect

- 选择候选章节。
- 滚动至候选证据。
- 高亮匹配原文。
- 多候选时展示选择界面。
- 匹配失败时展示原因和可执行动作。

### 15.6 失败模式

- 章节不存在。
- 原引用已变化。
- 缺少足够诊断证据。
- 无法匹配正文。
- 候选位置过多。
- 正文读取失败。

失败时必须说明已尝试的步骤、失败原因和下一步建议，不能只显示“定位失败”。

### 15.7 验收闭环

```text
选择诊断问题
→ 创建定位任务
→ 展示任务输入
→ 读取诊断证据
→ 查找章节
→ 匹配正文
→ 发布真实活动
→ 切换章节
→ 滚动并高亮原文
→ 等待作者确认
→ 进入局部改写
```

## 16. 分阶段实施计划

### Phase 1：规格与产品骨架

- 创建 `task-centric-workbench` OpenSpec change。
- 定义产品信息架构。
- 定义任务、活动和 UI Effect DTO。
- 定义旧作与新书共用的 Task Runtime。
- Workflow Graph 默认轻量化。
- 新增当前任务卡。
- 底部消息流降噪为最近 2～3 条。
- 完整活动进入任务中心。

### Phase 2：真实任务活动

- Main 侧新增 Task Runtime。
- 新增 `task-activity-event` IPC。
- Renderer 新增统一 activity store，例如 `useTaskActivityStream()`。
- 迁移并最终移除对 snapshot 拼装活动的依赖。
- 实现 heartbeat。
- 实现暂停、中断、失败和等待作者状态。

### Phase 3：工作区响应

- 实现统一 UI Effect executor，例如 `useTaskUiEffects()`。
- 接入切章、滚动、高亮。
- 实现任务驱动的中栏状态和空状态。
- 左栏支持章节、问题及其他上下文切换。
- 右栏切换为任务助手。

### Phase 4：旧作重建闭环

按顺序接入：

1. 定位原文。
2. 生成局部改写。
3. Hunk 审核。
4. 正文落盘和 checkpoint。
5. 针对性复检。
6. 问题关闭。
7. 最终全书复检。

### Phase 5：新书创作能力

- 新书目标模板。
- 人物和世界观建立。
- 故事线与卷章规划。
- 章节场景设计。
- 章节初稿生成。
- 连贯性检查。
- 事实底稿自动更新。

## 17. 强制需求

以下要求作为后续 OpenSpec 和实现验收的 MUST 条款：

1. 所有进入 `running` 的用户任务必须发布作者可读的 activity stream。
2. 底部消息流必须常驻，并展示最近 2～3 条重要活动。
3. 长任务超过 2 秒没有新活动时必须发布基于真实状态的 heartbeat。
4. 活动必须按适用情况表达输入、执行、输出、工作区反馈和下一步。
5. 活动不得以内置 ID 或技术阶段代码作为主展示文本。
6. 模型交互必须展示可见输入摘要、输出摘要、结构化结果和引用。
7. 产品不得展示模型隐藏的 chain-of-thought。
8. 操作正文或创作产物的任务必须发布 UI Effect。
9. Renderer 必须响应 UI Effect，并在消息流中记录已产生的工作区变化。
10. UI Effect 至少覆盖切章、滚动、高亮、Diff、Hunk 审核和 checkpoint。
11. 当前任务卡必须明确任务目标、输入、执行方式、输出、状态和作者下一步。
12. Workflow Graph 默认采用轻量摘要，完整流程按需展开。
13. 任务失败时必须展示失败原因、已完成步骤和可执行的恢复动作。
14. `locate-source` 必须作为第一个真实端到端任务完成验收。
15. 底层任务模型不得假设项目一定存在旧小说，必须能够承载新书创作任务。

## 18. 验收标准

### 18.1 用户认知

在没有查看开发日志的情况下，作者可以从当前任务卡、工作区和消息流明确理解：

- 当前任务及其目的。
- 任务正在使用的上下文。
- 当前执行方式和进度。
- 已产生的结果。
- 工作区变化的原因。
- 是否需要作者决策。
- 下一步动作。

### 18.2 消息流

- 每个真实运行任务都有连续、可读的活动轨迹。
- 底部默认信息简洁，任务中心可查看完整历史。
- heartbeat 来自真实状态。
- 工作区更新事件与实际 UI 行为一致。
- 任务完成、失败、暂停和等待作者均有明确状态反馈。

### 18.3 模型可审计性

- 能查看模型任务目标和可见输入摘要。
- 能查看模型使用的上下文和引用。
- 能查看模型输出及结构化结果。
- 能区分原文事实、系统推断和模型生成内容。
- 能查看哪些内容被采用、拒绝或等待确认。
- 不暴露隐藏 chain-of-thought。

### 18.4 工作区反馈

涉及正文或产物的任务至少产生一种可见反馈：

- 切章。
- 滚动。
- 高亮。
- Diff。
- Hunk 审核。
- checkpoint。
- 新内容预览或插入。

### 18.5 可扩展性

- 旧作重建和新书创作共享 Task Runtime、消息流、任务中心、模型审计、UI Effect 和作者确认机制。
- 新任务类型通过新增 playbook 接入，而非复制一套工作台。
- 计划模板可以新增、调整和组合，不依赖固定 11 步流程。

## 19. 产品决策总结

Novel Agent 后续建设应遵循以下决策：

- 当前任务是工作台主叙事。
- 流程导航保留，但退居辅助位置。
- 底部消息流必须常驻，它是作者感知“产品还在工作”的核心机制。
- 消息流必须来自真实 Task Runtime，而不是长期依赖前端状态拼装。
- 活动流解释发生了什么，UI Effect 展示结果在哪里，两者必须一致。
- 模型交互需要可审计，但不展示隐藏推理过程。
- 旧作重建只是第一套计划模板，新书创作是架构从一开始就必须支持的正式场景。
- 第一阶段以 `locate-source` 打通真实端到端闭环，再逐步接入改写、审核、落盘、复检与新书创作任务。
